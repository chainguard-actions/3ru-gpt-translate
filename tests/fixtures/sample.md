# Hello World

This is a sample markdown file for testing the GPT Translate action.

## Introduction

This file is used to test optional parameters such as `basePath`, `max_tokens`, `seed`, and `systemPrompt`.
